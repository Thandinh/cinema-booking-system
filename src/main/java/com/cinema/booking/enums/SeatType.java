package com.cinema.booking.enums;

public enum SeatType {
    NORMAL,  // Ghế thường — price_multiplier = 1.0
    VIP,     // Ghế VIP   — price_multiplier = 1.5
    COUPLE   // Ghế đôi   — price_multiplier = 2.0
}
